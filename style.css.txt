/* Reset some Streamlit default CSS */
body {
    margin: 0;
    font-family: Arial, sans-serif;
}
.wrapper {
    display: flex;
    height: 100vh;
}

/* Sidebar styling */
.sidebar .sidebar-content {
    background-color: #8A2BE2;
    color: white;
    height: 100vh;
    overflow-y: auto;
    position: fixed;
    width: 300px; /* Adjust width as necessary */
}

/* Adjust main area */
.main {
    margin-left: 300px; /* Same as sidebar width */
    overflow-y: auto;
    height: 100vh;
}

/* Adjust the main block margins */
.block-container {
    padding: 2rem;
    width: calc(100% - 300px); /* Responsive width */
}
